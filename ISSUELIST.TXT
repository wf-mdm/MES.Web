1) 看板:

   未显示时间区间信息 ，我后续会在LINEINFO表中增加两个字段STARTDT,ENDDT,显示时候不要显示日期，只要显示24小时制的时间即可
   
   字段标题 操作工 => 更改为 "现场员工"
   字段标题 设备   => 更改为 "设备状态"
   字段标题 物料   => 更改为 "物料状态"
   字段标题 数据   => 更改为 "过程数据"

   颜色区分要加上

   员工字段单元格，当没有员工时，背景色为浅红，否则为黑底黄字
   设备状态字段单元格，F红色，R绿色，M/I黄色，其他白色
   物料状态字段单元格，有信息，且WARNSTAT字段值为OK，显示绿色，NG红色，其他白色
   过程数据字段单元格，无信息，显示白色，PROCRESULT字段值0，显示白色，P绿色，F红色
   放行状态字段单元格，无信息，显示白色，P绿色，F红色
  
   不良率饼图，目前没有按照实际的良品，不良品显示，整个显示灰色，图例：良品要显示为绿色，不良红色

2）生产线操作主页面

   鼠标放置在工站图表上，要求可配置POPUP显示项，目前要求显示7行：工站名，工单（工单号/料号/名称），投入数，产出数，当站当前序列号及wip状态(sn_stat)，缺料信息，数据。
                                           如果7行效果不好，可以考虑去掉投入数，产出数这两行

   切换到表格显示格式（产线信息窗）时，要把鼠标POPUP显示项也呈现在表格数据内容区（点击右上角的那个按钮切换成表格显示格式时候）


   如果可能，看能否把下方的COPYRIGHT 区域换成日志内容区

3）将原来的追溯查询页面挂上来，放在查询报表下
   
   e:\intelliMes\app_code\webDAL.cs 对应取数SQL及条件
   e:\intelliMes\reportPages\traceability.aspx;traceability.aspx.cs 对应网页及代码

4）启动停止生产线操作，需要美化和简化，尽量简单/整洁

5）降级调用指令不对，应该是DownGrade，请参见excel文件的"生产线操作_降级"工作表

6）返工调用指令参数传递不对，应该是: 

   Request.Parameters["rwk"]="0"; (目前传递的值是S，对应的应该是报废操作）
   Request.Parameters["code"]=原因代码;
   Request.Parameters["op"]=目的工序

   请参见excel文件的"生产线操作_返工"工作表


10) 工站操作主页面

   缺料信息，要显示在线余数, 请求数，给料数/时间显示出来，如果放不下，可以考虑只显示在线余数, 缺料请求数 ( 给料数可以不显示 ), 要颜色区分一下.

   标题：工站信息 =>改为 “工艺信息”

   
20) 系统配置
   
21) 用户角色分配

    最好显示 用户ID-姓名 ， 角色， 功能维持目前模式即可
    查询条件，可以考虑将用户ID加上（如果复杂度高，暂时可以不作）
    “增加”按钮点击后：
          弹出界面：字段标题，显示了英文的USERID , 要改为：用户ID, 且下拉选择框要显示 USERID - 用户名格式
    “删除”按钮点击后：
          弹出界面：增加一个信息字段，用户ID，现有功能不变

22）人员配置
    
    查询条件，将用户ID加上
    公司采用同一格式， 代码 - 描述呈现
    删除按钮点击后，出现界面里面，密码要隐藏显示，并且要显示用户ID字段

23）操作权限

    增加按钮点击出来的画面， 要允许用户ID手动输入增加（允许不在下拉框内）
                             产线，工序，工站，均弄成下拉选择模式，且必须要允许选项“ALL”
    字段标题CAPLEVEL要改为中文，意思：技能等级，目前为ABC三类，实质系统没有区分，可以定义A,或B,或C
    
    字段标题COMMENTS要改为中文，意思：备注

24）生产线配置，本项目可以直接隐藏，我后台配置

    新增/编辑时候，保存按钮无效

25）工序配置，

    增加按钮点击后弹出画面，一个bug: 输入的显示名称被清除，但是点击提交的时候又提示必须要输入。

    编辑按钮直接死掉

26）工站配置，

    新增，编辑，显示名称输入无效，但是提示必须要输入

27）工艺参数配置

    参数集ID要过滤，只显示PARAM_TYPE='PRD'的参数，保存的时候，也要将此字段值设定为PRD

    同时发现另一个问题，当页面宽度超出屏幕宽度时候，没有左右滚动条

    参数类型，从eng_codecfg里面取SELECT CODEID,CODEDESC FROM ENG_CODECFG WHERE CODENAME='ENGPARMTYPE' 
  
    编辑按钮弹出画面，修改后保存，出错

        新增，编辑时候，产线，工序，工站，均弄成下拉选择模式，且必须要允许选项“ALL”


28）质量代码配置

    编辑按钮弹出画面，类型字段下拉没有值

   

29）BOM配置

    表头编辑的时候，工艺参数集，增加过滤条件PARAM_TYPE='PRD'

    BOM清单，增加按钮，点击没有反应
    “序列号管理”字段，要提供下拉选项1，2，3，4，5，..9,A,B,C,....，表示的是COMPn的这个n，第几个关键件

30) SOP配置

   “选择文件”按钮，点击后反应慢，而且选择了目标文件后，点击提交按钮时候，被自动清空，然后提示SOP文件字段是必须的
     预览功能，应该是 可以加载现有sop文件来预览的

40）查询报表

    追溯查询挂上

    增加一张按生产线/时间段/统计的报表，后续我提供数据源

51）session静默一段时间后，无法操作，界面出错，但是可以重新连接 （浏览器地址栏那里变不过来）


   
   